server:
  port: 9990
spring:
  datasource:
    name: druidDataSource
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://rm-wz963ju312wgv19502o.mysql.rds.aliyuncs.com:3306/zeus_finance?serverTimezone=GMT%2B8&characterEncoding=utf-8&useSSL=false
      username: mozi_root
      password: Cd40k1SKIXBQ
  # 配置redis环境
  redis:
    host: daiyifan-mozi.redis.rds.aliyuncs.com
    port: 6379
    username: r-wz9k3k93gatet5ukhu
    password: Sw@1235454redis
    database: 0
    lettuce:
      pool:
        max-active: 8 # 连接池最大连接数（使用负值表示没有限制）
        max-wait: -1ms # 连接池最大阻塞等待时间（使用负值表示没有限制）
        max-idle: 8 # 连接池中的最大空闲连接
        min-idle: 1 # 连接池中的最小空闲连接
    timeout: PT10S # 连接超时时间
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#  通过全局配置，设置实体类所对应表的统一前缀
#  global-config:
#    db-config:
#      id-type: auto // id策略全局配置
#      table-prefix: t_
